# dbsql

Простой npm-пакет для "подключения" к MySQL и выполнения "запросов". Внутри пакет использует GitHub API.

## Установка

Установите пакет глобально с помощью npm:

```bash
npm install -g dbsql